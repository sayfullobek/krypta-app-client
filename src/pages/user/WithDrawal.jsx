import {Button} from "reactstrap";

export const WithDrawal = () => {
    return (
        <div className={"w-100"} style={{height: '100vh'}}>
            <Button color={"primary m-2"} style={{position: 'absolute', bottom: '0', width: '96%'}}>
                address qo'shish
            </Button>
        </div>
    )
}